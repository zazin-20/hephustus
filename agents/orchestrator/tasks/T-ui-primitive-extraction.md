---
task: T-ui-primitive-extraction
role: worker
status: deferred
created: 2026-07-09
owner: orchestrator
---

# Task: Extract component-library patterns into React + Tailwind primitives (DEFERRED)

## Status
**Deferred by the user 2026-07-09.** Recorded so it is not lost; do NOT dispatch
until re-prioritized.

## Goal (when picked up)
Extract the canonical `agents/design-system/component-library.html` (42 units:
tokens, motion families, distortion layer, all hero surfaces) into React +
Tailwind v4 primitives in `frontend/src/`, per
`agents/design-system/DESIGN_LANGUAGE_STATUS.md` "What Must Happen Next".
Design System guides; Worker implements; token/component changes land in the
library first, then ship.

## Related deferred item
ASCIIText (React Bits, React + three.js) integration is also deferred — open
questions recorded in `agents/design-system/log.md` (placement + integration
approach; original paste was truncated, request the full component source).

## Dispatch
- Deferred. No agent assigned.
