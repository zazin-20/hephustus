---
issue_id: issue-018
worker: codex
status: done
created: 2026-07-03
reviewed_by: architect
---

## Summary

Implemented issue `#18` by replacing the authored `Profile` model with a reusable `Node` model plus scope-addressed runtime state.

Built:
- `hephaestus/store/nodes.py` for node persistence (`provider`, `tags`, attached `rules`, working dir, and forward-compatible governance fields for `inputs`, `outputs`, `skills`, `allowed_paths`, `allowed_tools`, and `context_policy`).
- `hephaestus/store/frozen_rules.py` for frozen-rule storage keyed by scope address with supersession on `(scope, scope_key, topic_key)` and lookup across `global -> machine -> workflow -> tag -> node`.
- Integration/service migration from role-based runs to provider/tag/node-based runs with per-run thread creation and scope-addressed execution contracts.
- Desktop bridge + frontend API/UI migration from profiles/roles/agent ids to nodes/providers/tags/node ids.
- Schema migration to `SCHEMA_VERSION = 3`, converting legacy `profiles` data into `nodes` and renaming runtime attribution columns from `agent_id` to `node_id`.

Files touched:
- Python core: `hephaestus/contract.py`, `hephaestus/desktop.py`, `hephaestus/identity.py`, `hephaestus/integration/__init__.py`, `hephaestus/integration/context.py`, `hephaestus/integration/routing.py`, `hephaestus/integration/runners.py`, `hephaestus/integration/service.py`, `hephaestus/okf_layout.py`, `hephaestus/rules/governance.py`, `hephaestus/store/corrections.py`, `hephaestus/store/db.py`, `hephaestus/store/frozen_rules.py`, `hephaestus/store/nodes.py`, `hephaestus/store/runs.py`, `hephaestus/store/threads.py`, `hephaestus/store/trace.py`, `hephaestus/store/violations.py`.
- Frontend: `frontend/src/App.jsx`, `frontend/src/api.js`, `frontend/src/components/Coordinator.jsx`, `frontend/src/components/RunAgent.jsx`, `frontend/src/components/Toast.jsx`, `frontend/src/mock.js`.
- Tests: `tests/test_contract.py`, `tests/test_corrections.py`, `tests/test_desktop_agent.py`, `tests/test_frozen_rules.py`, `tests/test_governance.py`, `tests/test_identity.py`, `tests/test_integration.py`, `tests/test_nodes.py`, `tests/test_store.py`, `tests/test_threads.py`, `tests/test_trace.py`, `tests/test_violations_store.py`.
- Removed: `hephaestus/store/profiles.py`, `tests/test_profiles.py`.

Verification:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest -q`
  - Result: `166 passed`
- `cmd /c npm --prefix frontend ci`
  - Installed frontend dependencies from `package-lock.json`
- `cmd /c npm --prefix frontend run build`
  - Result: production build succeeded
