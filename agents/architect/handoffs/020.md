---
issue_id: issue-020
worker: codex
status: done
created: 2026-07-04
reviewed_by: architect
---

## Summary

Implemented issue `#20` by adding a file-backed workflow graph model that round-trips under `agents/workflows/` and validates authored graph structure.

Built:
- `hephaestus/workflows.py` with typed `Workflow`, `Placement`, `Edge`, and `Guard` models plus YAML/JSON `save_workflow` and `load_workflow`.
- Validation for unknown placement references, duplicate placement ids, and unguarded cycles while allowing guarded cycles per ADR-0001.
- `hephaestus/okf_layout.py` support for the canonical `agents/workflows/` path.
- OKF scaffolding coverage so new workspaces include the workflows directory.

Files touched:
- Python core: `hephaestus/okf_layout.py`, `hephaestus/workflows.py`.
- Tests: `tests/test_okf_layout.py`, `tests/test_workflows.py`, `tests/test_workspace.py`.

Verification:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest --basetemp=.pytest_tmp`
  - Result: `185 passed, 0 failed, 0 skipped`
