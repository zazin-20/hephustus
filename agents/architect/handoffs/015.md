## Summary

Issue #15 is now complete around the provider-registry seam.

- Restored `hephaestus/integration/runners.py` with the generic execution scaffolding that other modules still depend on: `AgentTask`, `AgentEvent`, `AgentRunner`, `EchoRunner`, `ClaudeRunner`, `CodexRunner`, `build_codex_argv`, and `_extract_target_path`.
- Trimmed the old provider-specific free helpers out of `runners.py`. Claude/Codex raw-event normalization and flag resolution now live on providers via `hephaestus/integration/providers.py`, while the restored runners accept injected normalizers/flag resolvers and keep only generic process/SDK execution behavior.
- Updated `AgentService` to read the provider registry directly, accept an injected `ProviderRegistry` for tests, default its runner map from the registry, and still preserve built-in `Tool` compatibility for Claude/Codex callers.
- Removed the stale `ROLE_TOOL` export from `hephaestus/integration/__init__.py`.
- Adapted provider-related tests to exercise the new provider-owned normalization/flags, preserved Codex JSONL decoding coverage through `CodexRunner.decode_event_line()`, and kept fake-provider registry coverage intact.

Files touched:

- `hephaestus/integration/runners.py`
- `hephaestus/integration/service.py`
- `hephaestus/integration/routing.py`
- `hephaestus/integration/providers.py`
- `hephaestus/integration/__init__.py`
- `hephaestus/desktop.py`
- `tests/test_integration.py`
- `tests/test_trace.py`
- `tests/test_catalog.py`

Existing issue-scope files already changed in the worktree and retained as part of the provider-registry implementation:

- `hephaestus/catalog.py`
- `hephaestus/integration/adapters.py`
- `tests/test_provider_registry.py`

Final test result:

- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest -q`
- `169 passed, 6 warnings`
