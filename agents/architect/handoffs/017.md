## Summary

Built a unified `@@HEPHAESTUS@@` marker protocol parser in `hephaestus/handoff.py` and preserved legacy `parse_handoff()` behavior for existing embedded handoff JSON payloads.

Files touched for issue #17:
- `tests/test_handoff.py` adds protocol-marker red-step coverage for `handoff`, `skill_complete`, `distillation_candidate`, malformed markers, in-prose mentions, thinking-turn filtering, and trace command scanning.
- `hephaestus/handoff.py` now defines the unified marker dataclasses and parser entrypoints: `parse_marker()`, `parse_marker_from_turns()`, and `parse_marker_from_trace()`, while keeping `parse_handoff()` backward-compatible.

Test results:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest tests/test_handoff.py -q` -> `20 passed`
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest -q` -> `174 passed, 5 warnings`
