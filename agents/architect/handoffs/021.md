---
issue_id: issue-021
worker: codex
status: done
created: 2026-07-04
reviewed_by: architect
---

## Summary

Implemented issue `#21` by extracting `ExecutionContract` construction into a dedicated public resolution module and wiring `AgentService` to consume that derived-record seam instead of hand-filling the contract in service code.

Built:
- `hephaestus/integration/contract_resolution.py` with a public `resolve(...)` function that derives the governed `ExecutionContract` from node state, the assembled session context, and run metadata.
- `hephaestus/integration/service.py` now calls the resolver during `begin()` and no longer contains the ad-hoc `_contract(...)` field-by-field construction.
- `hephaestus/integration/__init__.py` now exports the resolver seam for integration consumers.
- `tests/test_contract_resolution.py` locks both acceptance paths: direct resolver output and `AgentService.begin()` parity with the public resolver.

Files touched:
- `hephaestus/integration/contract_resolution.py`
- `hephaestus/integration/service.py`
- `hephaestus/integration/__init__.py`
- `tests/test_contract_resolution.py`

Verification:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest tests\test_contract_resolution.py tests\test_integration.py --basetemp=.pytest_tmp`
  - Result: `33 passed`
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest --basetemp=.pytest_tmp`
  - Result: `188 passed, 5 warnings`

Notes:
- Local git commits from this sandbox are currently blocked because the linked worktree metadata lives under `C:\Users\kambala.jathin\Projects\hephustus\.git\worktrees\issue-021-execution-contract`, which is outside the writable sandbox roots for this session. The code and handoff are complete in the worktree, but commit creation needs to be retried from an environment that can write to the parent repo `.git` directory.
