---
issue_id: issue-030
worker: codex
status: done
created: 2026-07-07
reviewed_by: architect
---

## Summary

Implemented issue `#30` as a frontend-only reorganization with no backend, bridge, DAL, or test changes.

Files added:
- `frontend/src/components/Library.jsx`
- `frontend/src/components/Console.jsx`

Files changed:
- `frontend/src/App.jsx`
- `frontend/src/api.js`
- `frontend/src/components/NodeForm.jsx`
- `frontend/src/mock.js`

Files deleted:
- `frontend/src/components/Coordinator.jsx`

Moved capability check:
- `Library` is now the default tab and retains the full catalog surface: Nodes|Artifacts toggle, node CRUD including delete, artifact CRUD including delete, reuse of `NodeForm.jsx` and `ArtifactForm.jsx`, artifact detail pane, and preview-mode fallbacks for both nodes and artifacts.
- `Console` now hosts the full single-node interaction surface: node picker, thread list, conversation panel with include/exclude and Copy, trace panel, spawn card, and message composer.
- The live-agent behavior was preserved by copying the existing `onAgent` wiring and keeping `sendMessage`, `getTranscript`, `getTrace`, `parseHandoffMarker`, `evaluateSpawn`, and `setTurnIncluded` behavior unchanged.
- `Coordinator` was fully removed from the app nav and component tree; the nav is now `library`, `console`, `canvas`, `code`, `agent`.

Verification:
- `"/c/Program Files/nodejs/npm.cmd" --prefix frontend ci`
- `"/c/Program Files/nodejs/npm.cmd" --prefix frontend run build`
- Build passed.
- `grep -RIn -e Coordinator -e coordinator frontend/src` returned no remaining frontend references.
- Product-code diff stayed under `frontend/src/**`.

Commit status:
- Attempted the requested commit, but Git could not create the worktree lockfile (`index.lock`: permission denied), so the implementation is complete but uncommitted.
- Landed on branch `feat/030-coordinator-rehome` as commit `9575b14`.

## Architect review

**Verdict: APPROVE — ready to merge.** Reviewed commit `9575b14`. This is a clean,
capability-preserving split with no scope creep. Every cluster that lived in the
deleted `Coordinator.jsx` (1111 lines) is fully present in the two new files;
the shared authoring forms are reused, not duplicated; and product-code edits
are confined to `frontend/src/**`.

### Library capabilities (from Coordinator's left rail + detail pane)

| Capability | Status | Location |
| --- | --- | --- |
| Nodes\|Artifacts catalog toggle | present | `Library.jsx:394-412` (`catalogMode`) |
| Node list: select / Edit / Delete | present | `:435-468` (select/`openEditForm`/`removeNode`) |
| Artifact list: select / Edit / Delete | present | `:488-522` (select/`openEditArtifactForm`/`removeArtifact`) |
| Artifact detail pane (heading rules, best-practices/antipatterns/examples) | present | `:576-624` |
| Create via NodeForm / ArtifactForm | present | `:532-560` (imported `:17-18`) |
| List refresh after save | present | `saveNode`→`loadNodes` `:250`; `saveArtifact`→`loadArtifacts` `:308` |
| Preview-mode mock fallback | present | `NODES_MOCK` `:139`, `ARTIFACTS_MOCK` `:155` |

### Console capabilities (from Coordinator's right column)

| Capability | Status | Location |
| --- | --- | --- |
| Node picker / roster | present | `Console.jsx:345-372` |
| Thread list | present | `:388-407` (`loadThreadsFor`) |
| Conversation panel: per-turn include/exclude | present | `:452-463` (`toggleTurn`/`setTurnIncluded`) |
| Conversation Copy | present | `:416-423` (`copyConversation`/`copyToClipboard`) |
| Trace panel: collapsible, expandable rows (command/output/exit) | present | `:475-544` |
| Spawn card: green/amber gating, Confirm/Dismiss | present | `:546-590` |
| Message composer (issue + message + send) | present | `:592-618` (`submitMessage`) |
| Live `onAgent` wiring | present | `:131-176` |
| `activeRunId` / `activeAgentId` / `activeThreadId` refs | present | `:115-129`, set on send `:318-321` |
| Handoff-marker → evaluateSpawn flow | present | `:139-146` (`parseHandoffMarker`→`evaluateSpawn`→`setSpawnCard`) |

### Shared-form reuse (not duplicated)

- `NodeForm` and `ArtifactForm` are imported into `Library.jsx` (`:17-18`), not
  re-implemented. `ArtifactBindingEditor` is used only inside `NodeForm`
  (`NodeForm.jsx:2,326,333`) and is reached transitively — no duplication.
- Console imports none of the authoring forms (correct — it hosts no CRUD).

### Scope / regression checks

- `App.jsx`: nav is `['library','console','canvas','code','agent']`, default view
  `library`, both components imported/rendered, no dead `Coordinator` import — confirmed.
- `api.js`: only two comment lines changed (`Coordinator` → `Library`/`Library + Console`);
  no logic change — confirmed.
- `NodeForm.jsx`: single copy string `"desktop coordinator"` → `"desktop app"`;
  the #349733c `RULE_LABELS` rule-label work is intact (`NodeForm.jsx:10,259`) — confirmed.
- `mock.js`: `COORDINATOR_MOCK` → `NODES_MOCK` + new `ARTIFACTS_MOCK`; consistent
  with Library imports; zero remaining `COORDINATOR_MOCK`/`Coordinator` references
  anywhere in `frontend/src` — confirmed.
- `git show 9575b14 --stat`: product edits are entirely `frontend/src/**` (plus the
  handoff + worker-log docs). Nothing under `hephaestus/**` or tests changed — confirmed.

**No drops. No scope creep.** Approved for QA / merge.
