---
issue_id: issue-022
worker: codex
status: in-progress
created: 2026-07-04
reviewed_by: architect
---

## Summary

Partial implementation of issue `#22`, recovered and committed by the
Orchestrator after the Codex session ended without writing its own handoff
(no session-limit message observed; the job's `task-worker` process was
simply gone by the time this was checked — likely ran out of turns mid-task).

**Done (acceptance criteria 1 & 2 of 4):**
- `hephaestus/skills.py`: `SkillReference` + `resolve_skill_ref(s)` — resolves
  `skill:<id>`, a bare registry name, or an explicit `agents/skills/*.md`
  path against the OKF `skills_dir` (`agents/skills/`).
- `hephaestus/okf_layout.py`: added `skills_dir` property, `skill_path(id)`
  helper, and included `skills_dir` in OKF scaffolding.
- `hephaestus/integration/context.py` / `service.py`: `build_session_context`
  now accepts `skills: list[str]`, resolves and injects each skill's
  playbook text into the session's system prompt under a `# Skills` section
  (cross-provider — this happens in the shared context-assembly seam, not
  per-provider code). `AgentService` passes `node.skills` through (the
  `Node.skills` field already existed from an earlier issue).
- Tests: `tests/test_skills.py` (reference resolution), plus
  `test_context_injects_skill_playbooks`,
  `test_context_reports_missing_skill_playbooks`, and
  `test_service_injects_node_skill_playbooks` in `tests/test_integration.py`.

**NOT done (acceptance criteria 3 & 4 of 4) — still needed:**
- No obligation/exit-gate concept exists anywhere in the repo yet (confirmed
  via repo-wide grep for `obligation`/`exit_gate` — zero matches outside this
  note). The issue calls for: a node can opt in to an *enforced* skill
  obligation; at exit, a gate checks the run's trace for that skill's
  completion marker (reusing the existing `@@HEPHAESTUS@@` marker-protocol
  parser from issue #17) and fails the run if the marker is absent.
- No tests exist for "obligation passes when marker present, fails when
  marker absent."

Files touched (committed):
- `hephaestus/skills.py` (new)
- `hephaestus/okf_layout.py`
- `hephaestus/integration/context.py`
- `hephaestus/integration/service.py`
- `tests/test_skills.py` (new)
- `tests/test_integration.py`
- `tests/test_okf_layout.py`
- `tests/test_workspace.py`

Verification:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest --basetemp=.pytest_tmp`
  - Result: `193 passed` (independently verified by the Orchestrator)

## Next step

Redispatch a follow-up Codex worker into this same worktree/branch
(`feat/022-skill-registry`) to implement the obligation/exit-gate piece on
top of this commit — do not restart from scratch, the skill-injection half
is solid and tested.
