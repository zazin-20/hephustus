---
issue_id: issue-028
worker: codex
status: done
created: 2026-07-07
reviewed_by: architect
---

## Summary

Implemented full node authoring/editing for issue `#28` across the DAL, desktop bridge, and desktop UI.

Changed files:
- `hephaestus/store/nodes.py`
- `hephaestus/desktop.py`
- `frontend/src/api.js`
- `frontend/src/components/Coordinator.jsx`
- `frontend/src/components/WorkflowCanvas.jsx`
- `frontend/src/components/NodeForm.jsx`
- `tests/test_nodes.py`
- `tests/test_desktop_agent.py`

Test count:
- Before: `211` collected
- After: `213` collected
- Verification: full backend suite passed with `213 passed`

## Changes

- Added `update_node(...)` to the typed node DAL, covering every mutable `Node` field and rewriting the identity card with the same capability derivation used on create.
- Widened bridge node creation to forward the full `Node` contract, return full node payloads, and added bridge `update_node`.
- Widened `frontend/src/api.js` to keep legacy 7-argument `createNode(...)` calls working while supporting the extra contract fields, plus added `updateNode(...)`.
- Added a shared `NodeForm` UI with list editors for `inputs`, `outputs`, `skills`, `skill_obligations`, `allowed_paths`, and `allowed_tools`.
- Wired create/edit flows into the coordinator roster and the workflow-canvas node palette, with prefilled edit state and node-list refresh after save.
- Kept `context_policy` plumbed through create/update while rendering it disabled/read-only with the reserved-runtime-effect label from the issue.

## Verification

- `C:/Users/kambala.jathin/Projects/hephustus/agents/.venv/Scripts/python.exe -m pytest`
  Result: `213 passed`
- `C:/Program Files/nodejs/npm.cmd --prefix frontend run build`
  Result: blocked by environment because `frontend/node_modules` is absent and `vite` is not installed in the worktree

## Notes

- A generated untracked directory under `sample/agents/identities/` was left untouched because removing it was blocked by sandbox policy; it was not staged.

## Architect review

**Reviewed 2026-07-07 (commit `4e76fc7`, branch `feat/028-node-authoring`). Verdict: APPROVE → ready for QA.**

Per-criterion:

1. **DAL `update_node`** — PASS. Updates every mutable field; preserves
   `node_id` and `created_at` (re-read via `get_node(...).created_at`); returns
   the updated `Node`; card kept in sync through the new shared
   `_write_identity_card` helper (same `default_capabilities(tags)` derivation
   as create). `create_node`/`get_node`/`list_nodes`/`delete_node` unchanged
   apart from `create_node` now calling the extracted helper (identical output).
2. **Bridge** — PASS. `create_node` accepts + forwards the 7 fields and returns
   the full payload via `_node_payload` (`{**asdict(node), "status": "idle"}`);
   `update_node` added and round-trips (covered by
   `test_bridge_create_update_and_list_nodes_round_trip_full_contract`).
3. **api.js** — PASS. `createNode` widened with defaulted params; `updateNode`
   added; both behind the `window.pywebview?.api?.…` guard.
4. **UI (`NodeForm.jsx` + wiring)** — PASS. Shared create/edit form with
   add/remove `ListEditor` for all six list fields plus existing fields; edit
   pre-fills via `formFromNode`; list refreshes after save (`loadNodes` in the
   coordinator, `refreshAvailableNodes` on the canvas).
5. **`context_policy`** — PASS. Plumbed through create/update; control rendered
   `disabled readOnly` with the reserved "no runtime effect … T-prompt-compression"
   label; no free-form value is accepted.
6. **Coordinator refactor (key risk)** — VERIFIED, no dropped behavior. The old
   inline create-form was extracted into `NodeForm.jsx` and wired into both the
   coordinator roster and the canvas palette. Confirmed survivors: roster still
   lists nodes; `removeNode` still defined (`Coordinator.jsx:384`) and wired to
   the Delete button (now in its own row, `:541`); `loadNodes` intact (`:219`);
   model selection + effort gating relocated into `NodeForm` (`selectModel` /
   `effortOptions`); rule toggle relocated (`toggleRule`); directory browse
   relocated (`browseDirectory` now returns the chosen path, passed as
   `onPickDirectory`). New Edit button added. Nothing silently disappeared.
7. **Regression/invariants** — PASS. Test changes are additive only (no test
   weakened/deleted); `.hephaestus/state.db` still written solely through the
   typed DAL; product code untouched outside `store/nodes.py`, `desktop.py`,
   `frontend/src/api.js`, and the frontend node-form components. `agents/worker/log.md`
   is a per-role log, in-bounds.

**Latent observation (non-blocking, no change requested):** `update_node`
rewrites the identity card with `sessions=[]`, so editing a node would discard
accumulated session provenance. This mirrors `create_node` and is currently
harmless — `append_session` has no live producer (only `tests/test_identity.py`
calls it). Flag for when session recording is wired into a runtime path.

**Out-of-scope leftover:** the untracked `sample/agents/identities/` directory
(noted above) is not part of #28; the Worker's sandbox blocked its removal.
Left for the Orchestrator to clean up; it is not staged in `4e76fc7`.
