---
issue_id: issue-016
worker: codex
status: done
created: 2026-07-03
reviewed_by: []
---

## Summary

Built `hephaestus.artifact_spec` to load ArtifactSpec markdown files, parse frontmatter plus named `##` sections, compile shipped predicate calls (`has_field`, `has_section`, `non_empty`, `min_items`, `matches`, plus `has_<name>()` aliases), and deterministically check artifact documents by running predicate-backed rules through the shared registry.

Files touched:
- `hephaestus/artifact_spec.py`
- `tests/test_artifact_spec.py`
- `agents/architect/handoffs/016.md`

Test results:
- Targeted artifact-spec tests: `3 passed`
- Full suite: `169 passed, 6 warnings`
