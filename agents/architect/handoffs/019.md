---
issue_id: issue-019
worker: codex
status: done
created: 2026-07-03
reviewed_by: architect
---

## Summary

Implemented issue `#19` by replacing the old directive-only session assembly with clean-slate node context assembly that layers scoped directives, declared artifacts/specs, and same-node replay within a workflow run.

Built:
- `hephaestus/integration/context.py` now assembles context from scope-addressed frozen rules (`global -> machine -> workflow -> tag -> node`), tag directive files, declared input artifacts, declared output specs, and prior included turns from the same node thread only.
- `hephaestus/integration/service.py` now resolves session context after the node thread identity is known, so replay is scoped to the correct `(node, workflow_run, placement)` thread instead of leaking across runs or nodes.
- `tests/test_integration.py` now locks the acceptance path: first run sees layered directives plus declared artifact/spec content, second run replays only that node's own prior turns, and another node in the same workflow run does not inherit that transcript.

Files touched:
- `hephaestus/integration/context.py`
- `hephaestus/integration/service.py`
- `tests/test_integration.py`

Verification:
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest tests\test_integration.py --basetemp=.pytest_tmp`
  - Result: `31 passed`
- `C:\Users\kambala.jathin\Projects\hephustus\agents\.venv\Scripts\python.exe -m pytest --basetemp=.pytest_tmp`
  - Result: `181 passed, 5 warnings`

Notes:
- The live `gh issue view 19 --repo zazin-20/hephustus` fetch was attempted first but blocked by network restrictions in this environment, so implementation followed the issue body embedded in the dispatch.
